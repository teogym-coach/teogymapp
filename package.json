{
  "name": "ptjournal",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@sentry/react": "^10.62.0",
    "html2canvas": "^1.4.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "recharts": "^2.12.7",
    "web-vitals": "^3.5.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "CI=false react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "regression": "node scripts/regression-check.js",
    "test:rules": "firebase emulators:exec --only firestore \"npx mocha --timeout 30000 --exit tests/rules/firestore.rules.test.mjs\""
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "chai": "^6.2.2",
    "firebase": "^10.14.1",
    "mocha": "^11.7.6"
  }
}
