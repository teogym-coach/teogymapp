// Cloud Functions are intentionally unused.
//
// The member app is designed for Firebase Spark plan operation and relies only on
// Firebase Auth + Firestore. Member lookup happens through members.memberUid, so
// the legacy member lookup index and callable creator were removed.
